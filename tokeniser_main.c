#include "tokeniser.h"
#define MAXCHAR 1000


int main(){
    char tokens[MAXCHAR][MAXCHAR];
    tokenise(tokens);
    //char *token_ptr = token; 
    //printf("\n\nMain has retreived: %s", token_pointer);
    
    //writeToFile(tokens);
}
